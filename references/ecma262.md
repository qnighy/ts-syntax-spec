# How to read ECMA-262

The submodule is located at references/ecma262.

## Important files

```
spec.html # the specification.
```

## Structure

- The element opened by `<emu-clause id="sec-ecmascript-language-expressions">` includes the syntax of expressions.
- The element opened by `<emu-clause id="sec-ecmascript-language-statements-and-declarations">` includes the syntax of statements and declarations.
- The element opened by `<emu-clause id="sec-ecmascript-language-functions-and-classes">` includes the syntax of functions and classes.
- The element opened by `<emu-clause id="sec-ecmascript-language-scripts-and-modules">` includes the syntax of scripts and modules.
